## Environment variables (example)
# Do NOT put real secrets in this file. Copy to `.env.local` and fill values locally.

# Site
SITE_BASE_URL=http://localhost:4321

# Lead flow
LEAD_HMAC_SECRET=replace-with-random-32bytes-hex
PDF_DOWNLOAD_URL=https://storage.example.com/pdfs/guide.pdf
RATE_LIMIT_WINDOW_MS=120000
RATE_LIMIT_MAX=5

# Email provider (example: Resend)
RESEND_API_KEY=your-resend-api-key
RESEND_FROM=no-reply@example.com
RESEND_SUBJECT=AIHALO 資料ダウンロードリンク

# Google Service Account (Sheets)
# Prefer file-based credentials via GOOGLE_APPLICATION_CREDENTIALS.
# Ensure the referenced file lives under `credentials/` (gitignored).
GOOGLE_APPLICATION_CREDENTIALS=./credentials/sheets-service-account.json
SHEETS_SPREADSHEET_ID=your-spreadsheet-id

# Alternatively (split fields; avoid embedding multi-line private keys)
SHEETS_CLIENT_EMAIL=service-account@project.iam.gserviceaccount.com
SHEETS_PRIVATE_KEY_PATH=./credentials/sheets-private-key.pem

# Stripe (planned)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx
